import { PostFiltersPipe } from './post-filters.pipe';

describe('PostFiltersPipe', () => {
  it('create an instance', () => {
    const pipe = new PostFiltersPipe();
    expect(pipe).toBeTruthy();
  });
});
